{% extends "layout.njk" %}

{% macro typeIcon(type) -%}
  <img class="type" src="/icons/types/{{ type }}.png" alt="{{ type | capitalize }}-type">
{%- endmacro %}

{% macro damageSummary(damage) -%}
  <div class="damage" title="{{ damage | effectivenessSummary }}">
    <small>
      💥{{ damage | round(1) }}
      {{ caller() }}
    </small>
  </div>
{%- endmacro %}

{% block content %}
  <h1>
    Pokémoves
    <img src="/icons/gbl.png" alt="">
  </h1>
  <p>
    Move counts for Pokémon GO battles.
  </p>
  <p>
    <small><em>Last updated: {{ lastUpdated }}</em></small>
  </p>
  <h2>
    Settings
  </h2>
  <div>
    <input type="checkbox" id="show-deltas" name="show-deltas" class="setting">
    <label for="show-deltas">
      Show advanced energy deltas
    </label>
  </div>
  <div>
    <input type="checkbox" id="show-damage" name="show-damage" class="setting">
    <label for="show-damage">
      Show base damage (includes STAB)
    </label>
  </div>
  <div>
    <input type="checkbox" id="show-leagues" name="show-leagues" class="setting">
    <label for="show-leagues">
      Show attack stats (rank #1 and max)
    </label>
  </div>
  <script src="{{ resources['/js/features.js'] }}"></script>
  <div>
    <button id="use-dark-theme" class="theme-button setting">
      <img src="/icons/themes/dark.png" alt="">
      Switch to Dark theme
    </button>
    <button id="use-light-theme" class="theme-button setting">
      <img src="/icons/themes/light.png" alt="">
      Switch to Light theme
    </button>
  </div>
  <script src="{{ resources['/js/theme.js'] }}"></script>
  <input id="search" type="text" placeholder="Search"  style="visibility: hidden;" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false">
  {% for pokemon in list %}
    {% set isMew = pokemon.name === "Mew" %}
    <h2 data-name="{{ pokemon.name | lower }}">
      {{ pokemon.name }}{{ "*" if isMew }}
    </h2>
    {% if isMew %}
      <p>* Only some of Mew’s fast moves are shown.</p>
    {% endif %}
    <div class="leagues">
      {% for league in ["little", "great", "ultra", "master"] %}
        <div class="league">
          <img src="/icons/leagues/{{ league }}.png" alt="">
          <div class="cmp">
            <div class="league-name">
              {{ league | capitalize }}
            </div>
            <div class="rank-one-cmp">
              {{ pokemon.cmp[league].rankOne | fixed(1) }}
            </div>
            <div>
              <small>
                <strong>Max:</strong> {{ pokemon.cmp[league].max | fixed(1) }}
              </small>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
    <table>
      <tr>
        <th class="{{ pokemon.types | first }}">
          {% for type in pokemon.types %}
            {{ typeIcon(type) }} {{ "/" if not loop.last }}
          {% endfor %}
        </th>
        {% for fastMove in pokemon.counts[0].fastMoves %}
          {% set type = "hidden" if fastMove.name === "Hidden Power" else fastMove.type %}
          <th class="{{ type }}">
            <div>
              {{ typeIcon(type) }}
              {{ fastMove.name }}
            </div>
            <div>
              <small>
                +{{ fastMove.energy }} energy
                / {{ fastMove.turns }} turn{{ "s" if fastMove.turns > 1 }}
              </small>
            </div>
            {% set damage = (fastMove | adjustedDamage(pokemon)) %}
            {% call damageSummary(damage) %}
              ({{ (damage / fastMove.turns) | round(2) }} dpt)
            {% endcall %}
          </th>
        {% endfor %}
      </tr>
      {% for row in pokemon.counts %}
        {% set chargedMove = row.chargedMove %}
        {% set type = chargedMove.type %}
        <tr class="{{ type }}">
          <th>
            <div>
              {{ typeIcon(type) }}
              {{ chargedMove.name }}
            </div>
            <div>
              <small>
                {{ chargedMove.energy }} energy
              </small>
            </div>
            {% set damage = (chargedMove | adjustedDamage(pokemon)) %}
            {% call damageSummary(damage) %}
              ({{ (damage / -chargedMove.energy) | round(2) }} dpe)
            {% endcall %}
          </th>
          {% for fastMove in row.fastMoves %}
            <td>
              {% set firstCount = (fastMove.counts | first) %}
              <div>
                {% for count in fastMove.counts %}
                  {% if count === firstCount %}
                    {{ count }}
                  {%- else %}
                    <em>
                      {{ count -}}
                    </em>
                  {%- endif %}
                  {{- "," if not loop.last }}
                {% endfor %}
              </div>
              <div>
                {% set extraEnergy = firstCount * fastMove.energy + chargedMove.energy %}
                <small class="deltas">
                  Δ +{{ extraEnergy }} / -{{ fastMove.energy - extraEnergy }}
                </small>
              </div>
            </td>
          {% endfor %}
        </tr>
      {% endfor %}
    </table>
  {% endfor %}
  <script src="{{ resources['/js/search.js'] }}"></script>
  <script src="{{ resources['/js/scroll.js'] }}"></script>
{% endblock %}
